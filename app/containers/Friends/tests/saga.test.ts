/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import friendsSaga from '../saga';

// const generator = friendsSaga();

describe('friendsSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
