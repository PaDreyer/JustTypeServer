/*
 * Friends Messages
 *
 * This contains all the text for the Friends container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Friends';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Friends container!',
  },
});
