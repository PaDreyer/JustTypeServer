/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import betsSaga from '../saga';

// const generator = betsSaga();

describe('betsSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
